// == Import : npm
import React from 'react';

// == Import : local
import './header.scss';

const Navbar = () => (
  <div id="navbar">
    <a href="">un lien</a>
  </div>
);

// == Export
export default Navbar;
